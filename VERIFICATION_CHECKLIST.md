# Kimi Code Agent v0.4.0 真机验收清单

本清单覆盖 v0.4.0 中新接通的全部断链链路。按顺序执行，每完成一项在 `[ ]` 里打 `x`。

## 前置准备

```bash
# 1. 启动应用（从 DMG 或 release build）
cd /Users/eastbuy/Documents/ChatGPT/kimi\ 桌面\ agent/macos
swift run -c release KimiCodeAgent

# 2. 准备干净测试目录
mkdir -p /tmp/kimi-verify-040
cd /tmp/kimi-verify-040
git init
echo "# Test Project" > README.md
git add -A && git commit -m "initial"
```

**环境要求**：
- 已配置 `KIMI_API_KEY` 环境变量（或 app 首次启动会提示输入）
- 网络能访问 Kimi API（`api.moonshot.cn`）

---

## 1. 会话创建与项目绑定

### [ ] 1.1 强制项目选择器
- **操作**：点击侧栏"新建会话"或主界面的"开始新会话"按钮
- **预期**：必须弹出文件夹选择器，不能跳过
- **验证点**：选择 `/tmp/kimi-verify-040` 后侧栏会话卡片显示项目路径

### [ ] 1.2 侧栏项目分组
- **操作**：再创建一个会话，绑定不同目录（如 `~/Documents`）
- **预期**：侧栏按项目路径分组，每个组显示该项目的所有会话
- **验证点**：两个项目组之间有视觉分隔

---

## 2. 基础对话与流式合并

### [ ] 2.1 流式输出合并
- **操作**：在会话中输入 `在当前目录创建 test.txt，写入三行：alpha、beta、gamma`
- **预期**：
  - 助手回复流式出现，不是一次性全出
  - 同一条消息的流式片段合并为一个气泡，不是每个 delta 一个气泡
  - 工具调用（Write/Edit）出现为独立卡片（tool card），不嵌入文本气泡
- **验证点**：最终只有 1-2 个助手气泡 + 1 个 Write 工具卡

### [ ] 2.2 Reasoning 折叠
- **操作**：观察刚才回复中是否有 "思考中..." 或类似的折叠区域
- **预期**：
  - 如果引擎返回了 reasoning part（取决于提示词和模型），应显示为可折叠的"思考过程"卡片
  - 默认折叠，点击展开后显示完整推理内容
- **验证点**：折叠/展开切换正常，内容不截断

### [ ] 2.3 文件确认已创建
```bash
cat /tmp/kimi-verify-040/test.txt
# 应输出 alpha / beta / gamma 三行
```

---

## 3. 权限审批（三键 + patterns + "总是允许"）

### [ ] 3.1 审批卡出现
- **操作**：输入 `把 test.txt 的 beta 改成 BETA`
- **预期**：
  - 如果是首次编辑该文件，会弹出权限审批卡
  - 卡片显示：操作类型（Edit）、文件路径（`test.txt`）、三个按钮（拒绝/仅一次/总是允许）
  - patterns 字段显示 `["test.txt"]`（或通配符 `["*.txt"]`，取决于引擎策略）
- **验证点**：卡片内容清晰，patterns 不是空数组

### [ ] 3.2 "仅一次"审批
- **操作**：点击"仅一次"
- **预期**：
  - 审批卡消失
  - 引擎继续执行，编辑完成后助手确认
  - 检查文件内容已修改
- **验证点**：下次编辑同一文件会再次弹审批卡（因为是"仅一次"）

### [ ] 3.3 "总是允许"审批
- **操作**：输入 `在 test.txt 末尾追加一行 delta`，审批卡出现后点击"总是允许"
- **预期**：
  - 审批卡消失
  - 后续对 `test.txt` 的编辑不再弹审批卡（已记忆"总是允许"）
- **验证点**：再输入 `把 test.txt 的 BETA 改回 beta`，这次不弹卡直接执行

### [ ] 3.4 "拒绝"审批
- **操作**：输入 `创建 secret.env 文件`，审批卡出现后点击"拒绝"
- **预期**：
  - 审批卡消失
  - 助手回复类似"操作被拒绝"或停止工作
  - 文件未创建
- **验证点**：`ls /tmp/kimi-verify-040/` 没有 `secret.env`

---

## 4. 运行控制（停止、Steer、Follow-up）

### [ ] 4.1 停止按钮
- **操作**：输入一个长任务 `在 bigfile.md 里写 2000 字的 macOS 开发教程`，等助手开始输出后立即点击界面上的"停止"按钮
- **预期**：
  - 输出中断（不再有新 delta）
  - 助手气泡封口（不再显示 "正在输入..." 的动画）
  - 会话状态从 busy 变回 idle
- **验证点**：文件要么不存在，要么只写了部分内容（不是完整 2000 字）

### [ ] 4.2 Steer（插队修正）
- **操作**：输入 `创建 notes.md，写入三个标题`，等助手开始执行后（但还没完成），在输入框输入 `顺便在开头加一个总标题`，按回车
- **预期**：
  - Steer 消息立即本地回显为用户气泡
  - 引擎收到 steer 后调整当前 turn 的行为（可能重新生成或追加操作）
  - 最终 notes.md 包含总标题
- **验证点**：Steer 生效（文件内容符合两次指令的组合），不是被忽略

### [ ] 4.3 Follow-up（排队）
- **操作**：输入 `创建 task1.txt`，在助手回复完成前再输入 `创建 task2.txt`
- **预期**：
  - 第二条消息进入队列，不会打断第一个 turn
  - 第一个 turn 完成后，第二个 turn 自动开始
- **验证点**：两个文件都成功创建，且时序正确（不是并发冲突）

---

## 5. 会话历史与 revert

### [ ] 5.1 历史重建
- **操作**：关闭应用，重启后点击刚才的会话
- **预期**：
  - 所有历史消息（用户气泡 + 助手气泡 + 工具卡）完整显示
  - 工具卡状态（completed/failed）正确
- **验证点**：没有空白气泡，没有重复消息

### [ ] 5.2 Revert 最后一个 turn
- **操作**：输入 `创建 revert-test.txt，写入一行 will-revert`，等完成后点击该回复的"撤销"按钮（如果 UI 有）或输入 `/revert`
- **预期**：
  - 引擎回滚该 turn 产生的文件改动
  - `revert-test.txt` 被删除（或恢复到改动前状态）
- **验证点**：`ls /tmp/kimi-verify-040/` 没有 `revert-test.txt`

### [ ] 5.3 Unrevert（如果实现了）
- **操作**：点击"撤销的撤销"或输入 `/unrevert`
- **预期**：文件重新出现
- **验证点**：`cat /tmp/kimi-verify-040/revert-test.txt` 显示 `will-revert`

---

## 6. 面板功能（五个新面板）

### [ ] 6.1 Diff 面板
- **操作**：编辑 `test.txt` 后切换到"Diff"面板
- **预期**：显示 `git diff` 输出（红色删除行 + 绿色新增行），与 `git diff` 命令行输出一致
- **验证点**：不是空白，也不是报错

### [ ] 6.2 Files 面板
- **操作**：切换到"Files"面板
- **预期**：
  - 左侧显示项目目录树（类似 VS Code 的文件浏览器）
  - 点击某个文件，右侧显示内容预览
- **验证点**：目录树能展开/折叠，预览区显示正确文件内容

### [ ] 6.3 Browser 面板
- **操作**：
  - 输入 `创建 chart.html，写一个简单的 SVG 柱状图`
  - 等助手创建完后切换到"Browser"面板
- **预期**：
  - 如果引擎返回了 artifact（HTML/SVG），Browser 面板会渲染预览
  - 如果没有 artifact，面板显示"无可预览内容"或空状态
- **验证点**：面板不崩溃，有内容时能正常渲染

### [ ] 6.4 验证面板
- **操作**：切换到"验证"面板
- **预期**：显示工具调用的 Intent/Receipt 审计日志（Harness 层记录）
- **验证点**：
  - 至少有几条工具调用记录（Write/Edit/Read）
  - 每条记录显示：工具名、参数（部分）、结果（success/failure）
  - 不是 JSON raw dump，是人类可读的表格或列表

### [ ] 6.5 集成面板
- **操作**：切换到"集成"面板
- **预期**：显示 MCP 服务器状态和 Skills 列表
- **验证点**：
  - MCP 区域显示当前连接的服务器（如果有的话），每个服务器显示名称 + 状态（online/offline）
  - Skills 区域列出可用的 slash 命令（从项目 `.kimi-agent/` 或全局目录发现）
  - 没有配置时显示空状态，不是报错

---

## 7. 高级功能

### [ ] 7.1 Slash 命令补全
- **操作**：在输入框输入 `/`，观察是否有自动补全提示
- **预期**：
  - 弹出补全菜单，列出可用命令（如 `/revert`、`/compact`、`/help` 等）
  - 如果项目有自定义 skills，也会出现在列表里
- **验证点**：补全菜单响应键盘上下键，回车选中后命令插入输入框

### [ ] 7.2 Todo 清单
- **操作**：输入 `列出这个项目需要做的三件事`，等助手回复
- **预期**：
  - 如果引擎返回了结构化 todo（`question_card` 或类似类型的 part），会显示为待办清单卡片
  - 卡片内每个 item 可勾选（UI 交互，不影响引擎）
- **验证点**：todo 卡片与普通文本气泡视觉区分明显

### [ ] 7.3 问答卡（Question Card）
- **操作**：输入一个模糊需求 `帮我优化这个项目的性能`（故意不给上下文）
- **预期**：
  - 如果引擎返回了澄清问题（question_card），会显示为选择题卡片
  - 卡片内有多个选项按钮，点击后发送选择结果给引擎
- **验证点**：点击选项后引擎收到回复并继续

### [ ] 7.4 模型切换
- **操作**：在设置或输入框附近找到模型选择下拉菜单，切换到另一个模型（如从 `kimi-k2.7-code` 切到 `kimi-k3`）
- **预期**：
  - 下拉菜单显示从引擎 `/provider` 拉取的模型列表
  - 切换后下一条消息使用新模型
  - 不需要重启 app
- **验证点**：切换后输入新问题，观察回复风格是否变化（不同模型可能有细微差异）

### [ ] 7.5 Compact（历史压缩）
- **操作**：在一个有很多消息的会话里输入 `/compact`
- **预期**：
  - 引擎将部分历史消息压缩为摘要，释放上下文窗口
  - UI 显示压缩完成提示或直接更新历史（取决于实现）
- **验证点**：不报错，后续对话仍然正常

---

## 8. 异常与边界

### [ ] 8.1 断网重连
- **操作**：关闭 WiFi，输入一条消息，等 10 秒后重新打开 WiFi
- **预期**：
  - 初次请求失败后显示错误提示
  - 网络恢复后 SSE 自动重连（或下次操作时重连）
  - 不会导致会话卡死或需要重启 app
- **验证点**：重连后能继续正常对话

### [ ] 8.2 长时间 turn（30 分钟超时）
- **操作**：输入一个极长任务，人工等待或模拟（如果有耐心）
- **预期**：
  - 30 分钟后 Driver 超时
  - 自动调用引擎 abort
  - 会话返回 idle，不是永远 busy
- **验证点**：超时后能输入新消息（实际测试可选，太耗时可跳过）

### [ ] 8.3 引擎崩溃恢复
- **操作**：手动杀掉引擎进程（`pkill -9 kimi-agent`），然后在 app 里输入新消息
- **预期**：
  - app 检测到引擎断线
  - 自动重启引擎或提示用户手动重启
  - 重启后能继续对话
- **验证点**：不会因引擎挂掉而无法使用

---

## 9. 呈现层（深色模式、快捷键、视图模式）

### [ ] 9.1 深色模式切换
- **操作**：打开设置 →「外观」分类，依次切换 跟随系统 / 浅色 / 深色
- **预期**：
  - 切换即时生效，整个三栏界面（侧栏、对话、面板、终端）颜色同步变化
  - 「跟随系统」档随 macOS 系统外观变化
  - 重启应用后保持上次选择
- **验证点**：浅色/深色下代码块高亮、Diff 红绿配色均清晰可读，无对比度死区

### [ ] 9.2 快捷键清单
- **操作**：依次按下以下快捷键
- **预期**：

  | 快捷键 | 行为 |
  | --- | --- |
  | ⌘B | 折叠/展开侧栏 |
  | ⌘⇧T 或 ⌃` | 折叠/展开终端 |
  | ⌘⇧D | 切换 Diff 面板 |
  | ⌘⇧B | 切换浏览器面板 |
  | ⌘; | 打开/关闭 Side Chat |
  | ⌘\ | 关闭分屏焦点侧 |
  | ⌃O | 循环视图模式（标准/详细/精简） |
  | Esc | 停止当前运行中的 turn |
  | ⌘/ | 打开快捷键帮助浮层 |

- **验证点**：帮助浮层（⌘/）所列快捷键与本表一致，每条实际生效

### [ ] 9.3 可缩放三栏布局与折叠持久化
- **操作**：拖动侧栏/面板/终端之间的分隔条调整宽度；折叠侧栏和终端后重启应用
- **预期**：分隔条拖拽流畅，最小/最大宽度受限不拉爆布局；重启后折叠状态与宽度恢复
- **验证点**：重启后布局与退出前一致

### [ ] 9.4 视图模式三档
- **操作**：按 ⌃O 循环切换 标准 / 详细 / 精简
- **预期**：消息时间线密度随档位变化（精简档隐藏次要信息，详细档展示更多元数据）
- **验证点**：三档切换无内容丢失，切回标准档恢复原样

### [ ] 9.5 消息悬停操作与会话头部用量
- **操作**：悬停在任意助手消息上；观察会话头部
- **预期**：hover 出现复制/重新生成按钮且可用；会话头部显示本会话累计 token 与成本（价格配置后为金额）
- **验证点**：复制内容与消息原文一致；重新生成触发新 turn

---

## 10. 输入与上下文（附件、@提及、权限模式）

### [ ] 10.1 图片粘贴附件
- **操作**：截图后在输入框按 ⌘V
- **预期**：附件条出现图片缩略图；发送后引擎收到图片 file part，助手能描述图片内容
- **验证点**：助手回复确实引用了截图中的内容

### [ ] 10.2 拖拽附件
- **操作**：从 Finder 拖一个文本文件和一个图片文件到输入框
- **预期**：附件条显示两个附件；发送后文本文件以 file:// 注入、图片以 data URL 注入
- **验证点**：助手能读取文本文件内容并看到图片

### [ ] 10.3 @提及文件
- **操作**：在输入框输入 `@`，继续输入文件名片段（如 `@test`）
- **预期**：弹出模糊搜索浮层，列出项目内匹配文件；键盘上下选择、回车插入路径
- **验证点**：发送后助手按路径找到并读取了该文件

### [ ] 10.4 权限模式三档
- **操作**：在 composer 权限模式选择器依次切换 手动确认 / 自动接受编辑 / 计划模式
- **预期**：
  - 手动确认：编辑/命令照常弹审批卡
  - 自动接受编辑：文件编辑不再弹审批卡直接执行（bash 等高危仍按需审批）
  - 计划模式：引擎以 plan agent 运行，只产出方案不编辑文件
- **验证点**：计划模式下让助手"修改 test.txt"，其只给出计划、文件未被改动；切回手动确认后审批卡恢复

### [ ] 10.5 文件路径右键菜单
- **操作**：右键点击对话或面板中的文件路径
- **预期**：菜单含 文件面板打开 / 编辑器打开 / Finder 显示 / 复制路径 / 附加为上下文，各项均生效
- **验证点**：「附加为上下文」后输入框出现对应附件或路径引用

### [ ] 10.6 系统通知
- **操作**：确保设置中通知开启；发起一个长任务后切到别的应用等待完成
- **预期**：任务完成（忙→闲）时出现 macOS 系统通知，点击通知跳回对应会话；出现新待审批时同样有通知
- **验证点**：关闭设置中的通知开关后不再弹通知

---

## 11. 评审闭环（Diff 评论、文件编辑、权限卡预览、PR 状态条）

### [ ] 11.1 Diff 面板逐行评论 + 批量提交
- **操作**：让助手改动若干文件后打开 Diff 面板；点击某行添加评论，再点另一文件的某行添加第二条评论，按 ⌘Enter 批量提交
- **预期**：Diff 面板为左文件列表 + 右详情布局；评论以行内锚点标注；⌘Enter 把所有评论作为一条消息发给引擎（运行中走 steer，空闲走 prompt）
- **验证点**：助手回复确实针对评论位置做了修改

### [ ] 11.2 过期评论标注
- **操作**：添加评论后让助手（或手动）改动被评论的行，再打开评论
- **预期**：评论被标注为"已过期"（对应行已变化），不会静默指向错误位置
- **验证点**：过期标注视觉可辨，提交时仍能发送

### [ ] 11.3 AI 评审
- **操作**：工作区有未提交改动时点击 Diff 面板的「AI 评审」按钮
- **预期**：触发一次评审 turn，助手输出对当前 diff 的评审意见
- **验证点**：评审意见引用了真实改动内容，不是空泛模板

### [ ] 11.4 Files 面板编辑 + 冲突检测
- **操作**：在 Files 面板打开 `test.txt`，直接编辑并保存；随后用外部编辑器（如 VS Code）修改同一文件，再回到 Files 面板保存
- **预期**：
  - 正常编辑保存写回磁盘
  - 外部修改后保存时弹出磁盘冲突提示（mtime + 内容双因子检测），不会静默覆盖
- **验证点**：`cat test.txt` 内容与保存一致；冲突场景下用户可选择覆盖或放弃

### [ ] 11.5 Files 面板降级
- **操作**：在 Files 面板打开一个超过 64KB 的文本文件和一个二进制文件（如 .png）
- **预期**：两者以只读/不可编辑方式降级展示，不崩溃
- **验证点**：编辑控件禁用，有明确提示

### [ ] 11.6 权限卡 diff 预览
- **操作**：让助手编辑一个已有文件，观察弹出的审批卡
- **预期**：审批卡内嵌显示该次编辑的 unified diff 预览（红删绿增），无需切到 Diff 面板即可判断
- **验证点**：预览 diff 与实际写入的改动一致

### [ ] 11.7 PR/CI 状态条
- **前提**：本机已安装并登录 `gh` CLI；项目为带 GitHub remote 的仓库
- **操作**：在关联 PR 的分支上创建会话，观察 PR 状态条；推送一次提交触发 CI
- **预期**：状态条显示当前分支 PR 编号与状态；CI 运行中状态条轮询更新（约 30s），CI 结束后收到系统通知
- **验证点**：状态与 `gh pr status` 输出一致；未装 `gh` 的机器上状态条静默隐藏不报错

---

## 12. 编排与隔离（Worktree、后台任务、Side Chat、分屏）

### [ ] 12.1 Worktree 隔离会话
- **操作**：确认设置「会话」分类中 worktree 开关开启（默认开）；在 git 仓库项目中新建会话
- **预期**：
  - 会话工作目录为 `<项目>/.kimi/worktrees/<id>`，分支为 `kimi/session-<id>`
  - 侧栏会话卡片显示分支徽标
  - 会话中的文件改动不影响项目根工作区
- **验证点**：`git -C <项目> worktree list` 能看到新 worktree；`git -C <项目> status` 干净

### [ ] 12.2 Worktree 清理与回退
- **操作**：删除一个有未提交改动的 worktree 会话，选择清理；再在非 git 目录新建会话
- **预期**：有未提交改动时先警告，确认后删除 worktree；非 git 项目自动回退到项目根、不报错
- **验证点**：清理后 `git worktree list` 不再有该条目

### [ ] 12.3 后台任务面板
- **操作**：让助手启动一个后台任务（如监听、长构建），打开后台任务面板
- **预期**：面板聚合显示活动任务，含耗时与状态
- **验证点**：任务结束后状态转为完成；注意本版不提供单任务停止按钮（引擎无此通道）

### [ ] 12.4 Side Chat
- **操作**：在会话中按 ⌘; 打开 Side Chat，问一个与当前任务相关的补充问题，然后关闭
- **预期**：
  - Side Chat 基于当前会话 fork，能看到主会话上下文
  - 关闭即删除，侧栏会话列表不出现 Side Chat 会话
- **验证点**：主会话历史不受 Side Chat 影响

### [ ] 12.5 双会话分屏
- **操作**：按住 ⌘ 点击侧栏另一个会话
- **预期**：主区分为左右两列各显示一个会话；两列可各自独立对话；按 ⌘\ 关闭焦点侧恢复单会话
- **验证点**：分屏时面板区隐藏；两侧发送互不串话

---

## 13. 浏览器预览与 dev server

### [ ] 13.1 常驻浏览器面板
- **操作**：按 ⌘⇧B 打开浏览器面板，在地址栏输入一个公网 https 地址
- **预期**：面板内 WKWebView 直接渲染页面；前进/后退/刷新/外部打开按钮可用
- **验证点**：页面可交互（点击、滚动），不是静态截图

### [ ] 13.2 本地文件与链接打开
- **操作**：让助手生成 `chart.html`，在对话/Files 面板中将其「在浏览器面板打开」；右键一条 http(s) 链接选择「在浏览器面板打开」
- **预期**：本地 HTML/PDF/图片/视频在浏览器面板渲染；链接同面板打开
- **验证点**：工作区外路径的本地文件不会被加载（workingPath 边界）

### [ ] 13.3 dev server 最小版
- **前提**：项目有 package.json 且含 dev 或 start 脚本
- **操作**：在浏览器面板的 dev server 区域点击启动
- **预期**：
  - 按 lockfile 推断的包管理器启动 dev server
  - 输出中检测到 localhost 地址后浏览器面板自动导航到该地址
  - 面板显示输出尾部 50 行用于排障，可停止/重启
- **验证点**：页面正常加载且支持热更新；退出应用时 dev server 进程随之终止（`ps` 无残留）

---

## 验收完成标准

- **关键流程（1-4 节）全部通过**：会话创建、流式合并、审批、停止/Steer/Follow-up
- **至少 3 个面板（6 节）能正常打开且有数据**：Diff、Files、验证
- **至少 2 个高级功能（7 节）验证通过**：Slash 补全、模型切换
- **没有阻塞性 bug**：UI 不崩溃、会话不卡死、文件操作生效

如果有测试项失败，在该项下方记录：
```
- **实际结果**：[描述失败现象]
- **错误日志**：[如果有的话，贴控制台输出或截图]
```

---

## 完成后下一步

1. 把打勾的结果反馈给我（可以直接把这个 Markdown 文件贴回来）
2. 如果有失败项，我会帮你诊断和修复
3. 验收通过后，下一步可以：
   - 开始 Worktree 接入（引擎已支持，用户价值明确）
   - 补充文档（用户手册、架构图）
   - 或者处理其他优先级任务
